{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Hangman",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/hangman",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "externalConsole": true,
            "MIMode": "lldb",
            "preLaunchTask": "build"
        }
    ]
}
